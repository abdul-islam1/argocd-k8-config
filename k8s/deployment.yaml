apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: project
  name: project
spec:
  replicas: 2
  selector:
    matchLabels:
      app: project
  strategy: {}
  template:
    metadata:
      labels:
        app: project
    spec:
      containers:
      - image: abdul1s/jenkins-test:10
        name: project
        resources: {}
status: {}
